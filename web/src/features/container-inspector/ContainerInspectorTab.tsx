import { LoadingSpinner } from "@/components/LoadingSpinner";

/** Stub — real implementation in Phase 11. */
export function ContainerInspectorTab() {
  return <LoadingSpinner label="Phase 11 coming up" />;
}
