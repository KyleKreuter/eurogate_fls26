import { LoadingSpinner } from "@/components/LoadingSpinner";

/** Stub — real implementation in Phase 10. */
export function HistoricalAnalyticsTab() {
  return <LoadingSpinner label="Phase 10 coming up" />;
}
